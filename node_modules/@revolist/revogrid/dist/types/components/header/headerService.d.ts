import { RevoGrid } from "../../interfaces";
interface Config {
    canResize?: boolean;
    resize(sizes: RevoGrid.ViewSettingSizeProp): void;
}
export default class HeaderService {
    private target;
    private config;
    private source;
    get columns(): RevoGrid.ColumnDataSchemaRegular[];
    set columns(source: RevoGrid.ColumnDataSchemaRegular[]);
    constructor(target: string, columns: RevoGrid.ColumnDataSchemaRegular[], config: Config);
    resizeChange(canResize?: boolean): void;
    destroy(): void;
}
export {};
