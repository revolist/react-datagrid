import DataStore from '../store/dataSource/data.store';
import DimensionProvider from "./dimension.provider";
import { RevoGrid } from "../interfaces";
declare type RowDataSources = {
    [T in RevoGrid.DimensionRows]: DataStore<RevoGrid.DataType>;
};
export declare class DataProvider {
    private dimensionProvider;
    readonly stores: RowDataSources;
    constructor(dimensionProvider: DimensionProvider);
    setData(data: RevoGrid.DataType[], type: RevoGrid.DimensionTypeRow | RevoGrid.DimensionRowPin): void;
}
export {};
